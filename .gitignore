# Secrets & credentials — agents/ excluded (models.json contains inline API keys)
credentials/
agents/

# Runtime memory (SQLite)
memory/

# Agent session transcripts
agents/*/sessions/
**/*.jsonl

# Logs & queues
logs/
delivery-queue/
devices/
update-check.json

# Lock files & observability runtime
**/.clawhub/lock.json
observability/data/
observability/reports/

# Backups
openclaw.json.bak
openclaw.json.bak.*

# Workspace repos — managed as separate git repos
workspace/
workspace-*/

# Node modules (inside any workspace)
**/node_modules/

# Claude Code internal
.claude/

# Secrets (duplicated in openclaw.json env.vars, rotate before wider sharing)
.env.gmail
.env.*

# Plugin installs — redownloadable from marketplace
extensions/

# Inbound WhatsApp media — large + potentially sensitive
media/
